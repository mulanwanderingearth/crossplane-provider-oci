---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: toolsruntimedatabasetoolsdatabaseapigatewayconfigglobals.database.oci.upbound.io
spec:
  group: database.oci.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigGlobal
    listKind: ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigGlobalList
    plural: toolsruntimedatabasetoolsdatabaseapigatewayconfigglobals
    singular: toolsruntimedatabasetoolsdatabaseapigatewayconfigglobal
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigGlobal is the
          Schema for the ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigGlobals
          API. Provides the Database Tools Database Api Gateway Config Global resource
          in Oracle Cloud Infrastructure Database Tools Runtime service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigGlobalSpec
              defines the desired state of ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigGlobal
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  advancedProperties:
                    additionalProperties:
                      type: string
                    description: (Updatable) Advanced global properties.
                    type: object
                  certificateBundle:
                    description: (Updatable) The certificate bundle that describes
                      the SSL certicicate. Ignored if the httpsPort is 0.
                    items:
                      properties:
                        certificatePrivateKey:
                          description: (Applicable when type=FILENAME) (Updatable)
                            Describes a certificate private key file to be used with
                            SSL
                          items:
                            properties:
                              format:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The format of the file
                                type: string
                              path:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The path to the file
                                type: string
                            type: object
                          type: array
                        certificatePublic:
                          description: (Applicable when type=FILENAME) (Updatable)
                            Describes a certificate file to be used with SSL. Ignored
                            if the httpsPort is 0.
                          items:
                            properties:
                              format:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The format of the file
                                type: string
                              path:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The path to the file
                                type: string
                            type: object
                          type: array
                        type:
                          description: (Updatable) The type of the certificate.
                          type: string
                      type: object
                    type: array
                  databaseApiStatus:
                    description: (Updatable) ORDS database API is a database management
                      and monitoring REST API. Database Actions requires this feature.
                    type: string
                  databaseToolsDatabaseApiGatewayConfigId:
                    description: The OCID of a Database Tools database API gateway
                      config.
                    type: string
                  databaseToolsDatabaseApiGatewayConfigIdRef:
                    description: Reference to a Config in apmconfig to populate databaseToolsDatabaseApiGatewayConfigId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  databaseToolsDatabaseApiGatewayConfigIdSelector:
                    description: Selector for a Config in apmconfig to populate databaseToolsDatabaseApiGatewayConfigId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  documentRoot:
                    description: (Updatable) The location of the static resources
                      to be served under the / root server path.
                    type: string
                  globalKey:
                    description: The key of the global config.
                    type: string
                  httpPort:
                    description: (Updatable) Specifies the HTTP listen port. 0 disables
                      HTTP. Use of ports below 1024 requires elevated (root) privileges
                      and is generally discouraged; deployment on non-privileged ports
                      (1024–65535) is recommended.
                    format: int64
                    type: integer
                  httpsPort:
                    description: (Updatable) Specifies the HTTPS listen port. 0 disables
                      HTTPS. Use of ports below 1024 requires elevated (root) privileges
                      and is generally discouraged; deployment on non-privileged ports
                      (1024–65535) is recommended. ORDS will use a self-signed certificate
                      if a certificate bundle is not provided.
                    format: int64
                    type: integer
                  poolRoute:
                    description: (Updatable) How the target pool route value is determined
                      for a HTTP request.
                    type: string
                  poolRoutingHeader:
                    description: (Updatable) The request header name providing the
                      pool route value.
                    type: string
                  type:
                    description: (Updatable) The type of the certificate.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  advancedProperties:
                    additionalProperties:
                      type: string
                    description: (Updatable) Advanced global properties.
                    type: object
                  certificateBundle:
                    description: (Updatable) The certificate bundle that describes
                      the SSL certicicate. Ignored if the httpsPort is 0.
                    items:
                      properties:
                        certificatePrivateKey:
                          description: (Applicable when type=FILENAME) (Updatable)
                            Describes a certificate private key file to be used with
                            SSL
                          items:
                            properties:
                              format:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The format of the file
                                type: string
                              path:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The path to the file
                                type: string
                            type: object
                          type: array
                        certificatePublic:
                          description: (Applicable when type=FILENAME) (Updatable)
                            Describes a certificate file to be used with SSL. Ignored
                            if the httpsPort is 0.
                          items:
                            properties:
                              format:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The format of the file
                                type: string
                              path:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The path to the file
                                type: string
                            type: object
                          type: array
                        type:
                          description: (Updatable) The type of the certificate.
                          type: string
                      type: object
                    type: array
                  databaseApiStatus:
                    description: (Updatable) ORDS database API is a database management
                      and monitoring REST API. Database Actions requires this feature.
                    type: string
                  databaseToolsDatabaseApiGatewayConfigId:
                    description: The OCID of a Database Tools database API gateway
                      config.
                    type: string
                  databaseToolsDatabaseApiGatewayConfigIdRef:
                    description: Reference to a Config in apmconfig to populate databaseToolsDatabaseApiGatewayConfigId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  databaseToolsDatabaseApiGatewayConfigIdSelector:
                    description: Selector for a Config in apmconfig to populate databaseToolsDatabaseApiGatewayConfigId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  documentRoot:
                    description: (Updatable) The location of the static resources
                      to be served under the / root server path.
                    type: string
                  globalKey:
                    description: The key of the global config.
                    type: string
                  httpPort:
                    description: (Updatable) Specifies the HTTP listen port. 0 disables
                      HTTP. Use of ports below 1024 requires elevated (root) privileges
                      and is generally discouraged; deployment on non-privileged ports
                      (1024–65535) is recommended.
                    format: int64
                    type: integer
                  httpsPort:
                    description: (Updatable) Specifies the HTTPS listen port. 0 disables
                      HTTPS. Use of ports below 1024 requires elevated (root) privileges
                      and is generally discouraged; deployment on non-privileged ports
                      (1024–65535) is recommended. ORDS will use a self-signed certificate
                      if a certificate bundle is not provided.
                    format: int64
                    type: integer
                  poolRoute:
                    description: (Updatable) How the target pool route value is determined
                      for a HTTP request.
                    type: string
                  poolRoutingHeader:
                    description: (Updatable) The request header name providing the
                      pool route value.
                    type: string
                  type:
                    description: (Updatable) The type of the certificate.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.globalKey is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.globalKey)
                || (has(self.initProvider) && has(self.initProvider.globalKey))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigGlobalStatus
              defines the observed state of ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigGlobal.
            properties:
              atProvider:
                properties:
                  advancedProperties:
                    additionalProperties:
                      type: string
                    description: (Updatable) Advanced global properties.
                    type: object
                  certificateBundle:
                    description: (Updatable) The certificate bundle that describes
                      the SSL certicicate. Ignored if the httpsPort is 0.
                    items:
                      properties:
                        certificatePrivateKey:
                          description: (Applicable when type=FILENAME) (Updatable)
                            Describes a certificate private key file to be used with
                            SSL
                          items:
                            properties:
                              format:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The format of the file
                                type: string
                              path:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The path to the file
                                type: string
                            type: object
                          type: array
                        certificatePublic:
                          description: (Applicable when type=FILENAME) (Updatable)
                            Describes a certificate file to be used with SSL. Ignored
                            if the httpsPort is 0.
                          items:
                            properties:
                              format:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The format of the file
                                type: string
                              path:
                                description: (Applicable when type=FILENAME) (Updatable)
                                  The path to the file
                                type: string
                            type: object
                          type: array
                        type:
                          description: (Updatable) The type of the certificate.
                          type: string
                      type: object
                    type: array
                  databaseApiStatus:
                    description: (Updatable) ORDS database API is a database management
                      and monitoring REST API. Database Actions requires this feature.
                    type: string
                  databaseToolsDatabaseApiGatewayConfigId:
                    description: The OCID of a Database Tools database API gateway
                      config.
                    type: string
                  documentRoot:
                    description: (Updatable) The location of the static resources
                      to be served under the / root server path.
                    type: string
                  globalKey:
                    description: The key of the global config.
                    type: string
                  httpPort:
                    description: (Updatable) Specifies the HTTP listen port. 0 disables
                      HTTP. Use of ports below 1024 requires elevated (root) privileges
                      and is generally discouraged; deployment on non-privileged ports
                      (1024–65535) is recommended.
                    format: int64
                    type: integer
                  httpsPort:
                    description: (Updatable) Specifies the HTTPS listen port. 0 disables
                      HTTPS. Use of ports below 1024 requires elevated (root) privileges
                      and is generally discouraged; deployment on non-privileged ports
                      (1024–65535) is recommended. ORDS will use a self-signed certificate
                      if a certificate bundle is not provided.
                    format: int64
                    type: integer
                  id:
                    type: string
                  key:
                    description: A string that uniquely identifies a Database Tools
                      database API gateway config global settings resource.
                    type: string
                  metadataSource:
                    description: The RESTful service definition location.
                    type: string
                  poolRoute:
                    description: (Updatable) How the target pool route value is determined
                      for a HTTP request.
                    type: string
                  poolRoutingHeader:
                    description: (Updatable) The request header name providing the
                      pool route value.
                    type: string
                  timeCreated:
                    description: The time the resource was created. An RFC3339 formatted
                      datetime string.
                    type: string
                  timeUpdated:
                    description: The time the resource was updated. An RFC3339 formatted
                      datetime string.
                    type: string
                  type:
                    description: (Updatable) The type of the certificate.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
