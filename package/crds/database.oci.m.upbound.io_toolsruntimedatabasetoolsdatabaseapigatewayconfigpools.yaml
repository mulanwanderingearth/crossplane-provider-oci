---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: toolsruntimedatabasetoolsdatabaseapigatewayconfigpools.database.oci.m.upbound.io
spec:
  group: database.oci.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigPool
    listKind: ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigPoolList
    plural: toolsruntimedatabasetoolsdatabaseapigatewayconfigpools
    singular: toolsruntimedatabasetoolsdatabaseapigatewayconfigpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigPool is the
          Schema for the ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigPools API.
          Provides the Database Tools Database Api Gateway Config Pool resource in
          Oracle Cloud Infrastructure Database Tools Runtime service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigPoolSpec
              defines the desired state of ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigPool
            properties:
              forProvider:
                properties:
                  advancedProperties:
                    additionalProperties:
                      type: string
                    description: (Updatable) Advanced pool properties.
                    type: object
                  databaseActionsStatus:
                    description: (Updatable) Specifies to enable the Database Actions
                      feature.
                    type: string
                  databaseToolsConnectionId:
                    description: (Updatable) The OCID of the related Database Tools
                      connection. Specifies the Oracle Cloud Infrastructure database
                      tools connection ocid to build the connection pool from.
                    type: string
                  databaseToolsConnectionIdRef:
                    description: Reference to a ToolsDatabaseToolsConnection in database
                      to populate databaseToolsConnectionId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  databaseToolsConnectionIdSelector:
                    description: Selector for a ToolsDatabaseToolsConnection in database
                      to populate databaseToolsConnectionId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  databaseToolsDatabaseApiGatewayConfigId:
                    description: The OCID of a Database Tools database API gateway
                      config.
                    type: string
                  databaseToolsDatabaseApiGatewayConfigIdRef:
                    description: Reference to a Config in apmconfig to populate databaseToolsDatabaseApiGatewayConfigId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  databaseToolsDatabaseApiGatewayConfigIdSelector:
                    description: Selector for a Config in apmconfig to populate databaseToolsDatabaseApiGatewayConfigId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  displayName:
                    description: (Updatable) A user-friendly name. Does not have to
                      be unique, and it’s changeable. Avoid entering confidential
                      information.
                    type: string
                  initialPoolSize:
                    description: (Updatable) Specifies the initial size for the number
                      of database connections that will be created for the pool.
                    format: int64
                    type: integer
                  jwtProfileAudience:
                    description: (Updatable) Specifies the expected audience for the
                      JWT token. This value is used to validate the aud claim in the
                      JWT token.
                    type: string
                  jwtProfileIssuer:
                    description: (Updatable) Specifies the issuer of the JWT token.
                      This value is used to validate the iss claim in the JWT token.
                    type: string
                  jwtProfileJwkUrl:
                    description: (Updatable) Specifies the URL of the JSON Web Key
                      (JWK) that is used to verify the signature of the JWT token.
                    type: string
                  jwtProfileRoleClaimName:
                    description: (Updatable) Specifies the JSON pointer to the claim
                      in the JWT token that contains the roles of the users.
                    type: string
                  maxPoolSize:
                    description: (Updatable) Specifies the maximum number of database
                      connections allowed for the pool.
                    format: int64
                    type: integer
                  minPoolSize:
                    description: (Updatable) Specifies the minimum number of database
                      connections allowed for the pool.
                    format: int64
                    type: integer
                  poolRouteValue:
                    description: (Updatable) The pool route value provided in requests
                      to target this pool.
                    type: string
                  restEnabledSqlStatus:
                    description: (Updatable) Specifies whether the REST-Enabled SQL
                      service is active.
                    type: string
                  type:
                    description: (Updatable) The type of the Database Tools database
                      API gateway config pool sub resource.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  advancedProperties:
                    additionalProperties:
                      type: string
                    description: (Updatable) Advanced pool properties.
                    type: object
                  databaseActionsStatus:
                    description: (Updatable) Specifies to enable the Database Actions
                      feature.
                    type: string
                  databaseToolsConnectionId:
                    description: (Updatable) The OCID of the related Database Tools
                      connection. Specifies the Oracle Cloud Infrastructure database
                      tools connection ocid to build the connection pool from.
                    type: string
                  databaseToolsConnectionIdRef:
                    description: Reference to a ToolsDatabaseToolsConnection in database
                      to populate databaseToolsConnectionId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  databaseToolsConnectionIdSelector:
                    description: Selector for a ToolsDatabaseToolsConnection in database
                      to populate databaseToolsConnectionId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  databaseToolsDatabaseApiGatewayConfigId:
                    description: The OCID of a Database Tools database API gateway
                      config.
                    type: string
                  databaseToolsDatabaseApiGatewayConfigIdRef:
                    description: Reference to a Config in apmconfig to populate databaseToolsDatabaseApiGatewayConfigId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  databaseToolsDatabaseApiGatewayConfigIdSelector:
                    description: Selector for a Config in apmconfig to populate databaseToolsDatabaseApiGatewayConfigId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  displayName:
                    description: (Updatable) A user-friendly name. Does not have to
                      be unique, and it’s changeable. Avoid entering confidential
                      information.
                    type: string
                  initialPoolSize:
                    description: (Updatable) Specifies the initial size for the number
                      of database connections that will be created for the pool.
                    format: int64
                    type: integer
                  jwtProfileAudience:
                    description: (Updatable) Specifies the expected audience for the
                      JWT token. This value is used to validate the aud claim in the
                      JWT token.
                    type: string
                  jwtProfileIssuer:
                    description: (Updatable) Specifies the issuer of the JWT token.
                      This value is used to validate the iss claim in the JWT token.
                    type: string
                  jwtProfileJwkUrl:
                    description: (Updatable) Specifies the URL of the JSON Web Key
                      (JWK) that is used to verify the signature of the JWT token.
                    type: string
                  jwtProfileRoleClaimName:
                    description: (Updatable) Specifies the JSON pointer to the claim
                      in the JWT token that contains the roles of the users.
                    type: string
                  maxPoolSize:
                    description: (Updatable) Specifies the maximum number of database
                      connections allowed for the pool.
                    format: int64
                    type: integer
                  minPoolSize:
                    description: (Updatable) Specifies the minimum number of database
                      connections allowed for the pool.
                    format: int64
                    type: integer
                  poolRouteValue:
                    description: (Updatable) The pool route value provided in requests
                      to target this pool.
                    type: string
                  restEnabledSqlStatus:
                    description: (Updatable) Specifies whether the REST-Enabled SQL
                      service is active.
                    type: string
                  type:
                    description: (Updatable) The type of the Database Tools database
                      API gateway config pool sub resource.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.displayName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.displayName)
                || (has(self.initProvider) && has(self.initProvider.displayName))'
            - message: spec.forProvider.poolRouteValue is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.poolRouteValue)
                || (has(self.initProvider) && has(self.initProvider.poolRouteValue))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigPoolStatus
              defines the observed state of ToolsRuntimeDatabaseToolsDatabaseApiGatewayConfigPool.
            properties:
              atProvider:
                properties:
                  advancedProperties:
                    additionalProperties:
                      type: string
                    description: (Updatable) Advanced pool properties.
                    type: object
                  databaseActionsStatus:
                    description: (Updatable) Specifies to enable the Database Actions
                      feature.
                    type: string
                  databaseToolsConnectionId:
                    description: (Updatable) The OCID of the related Database Tools
                      connection. Specifies the Oracle Cloud Infrastructure database
                      tools connection ocid to build the connection pool from.
                    type: string
                  databaseToolsDatabaseApiGatewayConfigId:
                    description: The OCID of a Database Tools database API gateway
                      config.
                    type: string
                  displayName:
                    description: (Updatable) A user-friendly name. Does not have to
                      be unique, and it’s changeable. Avoid entering confidential
                      information.
                    type: string
                  id:
                    type: string
                  initialPoolSize:
                    description: (Updatable) Specifies the initial size for the number
                      of database connections that will be created for the pool.
                    format: int64
                    type: integer
                  jwtProfileAudience:
                    description: (Updatable) Specifies the expected audience for the
                      JWT token. This value is used to validate the aud claim in the
                      JWT token.
                    type: string
                  jwtProfileIssuer:
                    description: (Updatable) Specifies the issuer of the JWT token.
                      This value is used to validate the iss claim in the JWT token.
                    type: string
                  jwtProfileJwkUrl:
                    description: (Updatable) Specifies the URL of the JSON Web Key
                      (JWK) that is used to verify the signature of the JWT token.
                    type: string
                  jwtProfileRoleClaimName:
                    description: (Updatable) Specifies the JSON pointer to the claim
                      in the JWT token that contains the roles of the users.
                    type: string
                  key:
                    description: A system generated string that uniquely identifies
                      a pool sub resource.
                    type: string
                  maxPoolSize:
                    description: (Updatable) Specifies the maximum number of database
                      connections allowed for the pool.
                    format: int64
                    type: integer
                  minPoolSize:
                    description: (Updatable) Specifies the minimum number of database
                      connections allowed for the pool.
                    format: int64
                    type: integer
                  poolRouteValue:
                    description: (Updatable) The pool route value provided in requests
                      to target this pool.
                    type: string
                  restEnabledSqlStatus:
                    description: (Updatable) Specifies whether the REST-Enabled SQL
                      service is active.
                    type: string
                  timeCreated:
                    description: The time the resource was created. An RFC3339 formatted
                      datetime string.
                    type: string
                  timeUpdated:
                    description: The time the resource was updated. An RFC3339 formatted
                      datetime string.
                    type: string
                  type:
                    description: (Updatable) The type of the Database Tools database
                      API gateway config pool sub resource.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
