apiVersion: oci.m.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
  namespace: ${TARGET_NAMESPACE}
spec:
  credentials:
    source: Secret
    secretRef:
      name: oci-creds
      namespace: ${TARGET_NAMESPACE}
      key: credentials
