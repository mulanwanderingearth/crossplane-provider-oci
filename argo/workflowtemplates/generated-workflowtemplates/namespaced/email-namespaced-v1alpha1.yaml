
apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: oci-email-namespaced-v1alpha1-tests-template
spec:
  serviceAccountName: argo-workflow
  entrypoint: email-namespaced-tests-workflow
  volumes:
    - name: git-repo
      persistentVolumeClaim:
        claimName: git-repo-pvc
  arguments:
    parameters:
    - name: target_namespace
    - name: compartment_ocid
    - name: tenancy_ocid
    - name: create_compartment
      value: "true"
    - name: create_resources
      value: "true"
    - name: delete_resources
      value: "true"
  templates:
    - name: email-namespaced-tests-workflow
      inputs:
        parameters:
          - name: target_namespace
            value: "{{workflow.parameters.target_namespace}}"
      dag:
        tasks:
        - name: create-compartment
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_compartment}} == true"
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/identity/v1alpha1/compartment.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
              - name: envVars
                value: "COMPARTMENT_OCID={{workflow.parameters.compartment_ocid}}"
        - name: create-dkim
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-emaildomain]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/email/v1alpha1/dkim.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: create-emaildomain
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-compartment]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/email/v1alpha1/emaildomain.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: create-emailreturnpath
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-emaildomain]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/email/v1alpha1/emailreturnpath.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: create-sender
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-compartment]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/email/v1alpha1/sender.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: create-suppression
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/email/v1alpha1/suppression.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
              - name: envVars
                value: "TENANCY_OCID={{workflow.parameters.tenancy_ocid}}"
        - name: delete-dkim
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-dkim"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-dkim.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-dkim.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-dkim.outputs.parameters.resourceGroup}}"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: delete-emaildomain
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-emaildomain", "delete-dkim", "delete-emailreturnpath"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-emaildomain.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-emaildomain.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-emaildomain.outputs.parameters.resourceGroup}}"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: delete-emailreturnpath
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-emailreturnpath"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-emailreturnpath.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-emailreturnpath.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-emailreturnpath.outputs.parameters.resourceGroup}}"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: delete-sender
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-sender"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-sender.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-sender.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-sender.outputs.parameters.resourceGroup}}"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: delete-suppression
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-suppression"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-suppression.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-suppression.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-suppression.outputs.parameters.resourceGroup}}"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
